{
    "files.exclude": {
        "**/.git": true,
        "**/.DS_Store": true,
        "**/*.js": {
            "when": "$(basename).ts"
        },
        "**/*.jsx": {
            "when": "$(basename).tsx"
        },
        "**/*.map": {
            "when": "$(basename).map"
        } 
    }
}
